// Generated by scripts/update-build-metadata.mjs.
const CACHE_VERSION = 'vts-20260624-223549';
const APP_SHELL = [
  '/',
  '/404.html',
  '/admin.html',
  '/assets/admin-D1Gvfu0q.js',
  '/assets/skins/al-fatih-reflection-icon.webp',
  '/assets/skins/al-fatih-skin-icon.webp',
  '/assets/skins/alfred-idling-icon.webp',
  '/assets/skins/beastqueen-skin-icon.webp',
  '/assets/skins/beowulf-tass-legion-icon.webp',
  '/assets/skins/black-prince-skin-icon.webp',
  '/assets/skins/bleeding-steed-legion-iii-icon.webp',
  '/assets/skins/caesar-legion-iii-icon.webp',
  '/assets/skins/charles-the-great-reflection-icon.webp',
  '/assets/skins/charles-the-great-skin-icon.webp',
  '/assets/skins/cleopatra-vii-legion-i-icon.webp',
  '/assets/skins/edward-the-confessor-skin-icon.webp',
  '/assets/skins/immortal-guardian-tass-legion-icon.webp',
  '/assets/skins/immortal-skin-icon.webp',
  '/assets/skins/jade-eagle-dragon-icon.webp',
  '/assets/skins/jade-eagle-legion-iii-icon.webp',
  '/assets/skins/jade-rakshasa-skin-icon.webp',
  '/assets/skins/jeanne-darc-idling-icon.webp',
  '/assets/skins/king-arthur-arthur-pendragon-icon.webp',
  '/assets/skins/lionheart-skin-icon.webp',
  '/assets/skins/mary-tudor-skin-icon.webp',
  '/assets/skins/octavius-legion-ii-icon.webp',
  '/assets/skins/ramses-ii-tass-legion-icon.webp',
  '/assets/skins/rozen-blade-legion-ii-icon.webp',
  '/assets/skins/sky-breaker-skin-icon.webp',
  '/assets/skins/theodora-royal-icon.webp',
  '/CNAME',
  '/css/app.css',
  '/css/light-theme-overrides.css',
  '/css/mobile.css',
  '/css/ocr-dashboard.css',
  '/css/tailwind-build.css',
  '/css/tailwind-input.css',
  '/images/ac_camp.jpg',
  '/images/bonus_loyalty.png',
  '/images/boot/blazing-wing-right.webp',
  '/images/boot/dreamy-wing-left.webp',
  '/images/CM.png',
  '/images/heroes/catchup/reinforced-ragnar-placeholder.svg',
  '/images/logo.png',
  '/images/patches.jpg',
  '/images/processing_time.jpg',
  '/images/prod_1_3.jpg',
  '/images/prod_4_6.jpg',
  '/images/saved_units.png',
  '/images/strife/monsters/black-annis.jpg',
  '/images/strife/monsters/fordogreen.jpg',
  '/images/strife/monsters/gambosate.jpg',
  '/images/strife/monsters/noisy-noel.jpg',
  '/images/strife/monsters/pivana.jpg',
  '/images/strife/monsters/rabbit.jpg',
  '/images/strife/monsters/sasha.jpg',
  '/images/strife/monsters/savage-swordsman.jpg',
  '/images/strife/monsters/titanus.jpg',
  '/images/strife/monsters/trident-north-sea.jpg',
  '/images/strife/roc-strife-reference.png',
  '/images/WB.png',
  '/index.html',
  '/js/admin-auth-config.js',
  '/js/admin-page.js',
  '/js/app-builder.js',
  '/js/app-error-reporting.js',
  '/js/app-export.js',
  '/js/app-generator.js',
  '/js/app-hero-atlas.js',
  '/js/app-hero-tooltip.js',
  '/js/app-loading.js',
  '/js/app-research.js',
  '/js/app-shortcuts.js',
  '/js/app-strife.js',
  '/js/app-undo.js',
  '/js/app-whats-new.js',
  '/js/app.js',
  '/js/bug-widget.js',
  '/js/combo-counter-lookup.js',
  '/js/combo-counters.js',
  '/js/combo-share.js',
  '/js/combos-db.js',
  '/js/comments.js',
  '/js/constants.js',
  '/js/counter-db.js',
  '/js/firebase-sdk.js',
  '/js/firebase.js',
  '/js/game-time.js',
  '/js/hero-bonuses.js',
  '/js/heroes-data.js',
  '/js/heroes-info.js',
  '/js/i18n/ar.js',
  '/js/i18n/de.js',
  '/js/i18n/en.js',
  '/js/i18n/es.js',
  '/js/i18n/fr.js',
  '/js/i18n/id.js',
  '/js/i18n/kr.js',
  '/js/i18n/pt.js',
  '/js/i18n/ru.js',
  '/js/i18n/tr.js',
  '/js/i18n/zh.js',
  '/js/loyalty-calculator.js',
  '/js/maintenance-config.js',
  '/js/ocr-dashboard.js',
  '/js/ocr-engine.js',
  '/js/ocr-render.js',
  '/js/ocr-roster.js',
  '/js/ocr-shared.js',
  '/js/ocr-time-filter.js',
  '/js/player-profile.js',
  '/js/pwa-register.js',
  '/js/research-advanced.js',
  '/js/research-node-icons.js',
  '/js/roster-share.js',
  '/js/seo.js',
  '/js/skin-heroes-data.js',
  '/js/skins-db.js',
  '/js/state.js',
  '/js/strife-db.js',
  '/js/tech-db.js',
  '/js/translations.js',
  '/js/ui-bridge.js',
  '/js/user-data-portability.js',
  '/js/utils.js',
  '/robots.txt',
  '/site.webmanifest',
  '/sitemap.xml',
  '/tabs/admin.html',
  '/tabs/loyalty.html',
  '/workers/qwen-cors-proxy.js'
];
const MAX_CACHE_ENTRIES = 350;

function normalizedRequestKey(request) {
  const url = new URL(request.url);
  return url.origin === self.location.origin ? url.pathname : request;
}

async function batchAddUrls(cache, urls, batchSize) {
  for (let i = 0; i < urls.length; i += batchSize) {
    const batch = urls.slice(i, i + batchSize);
    await Promise.allSettled(batch.map((url) => cache.add(url)));
  }
}

async function trimCache(cache, maxEntries) {
  const keys = await cache.keys();
  if (keys.length <= maxEntries) return;
  // Evict oldest entries first (CacheStorage keys are insertion-ordered).
  const toDelete = keys.slice(0, keys.length - maxEntries);
  await Promise.all(toDelete.map((request) => cache.delete(request)));
}

self.addEventListener('install', (event) => {
  self.skipWaiting();
  event.waitUntil(
    caches.open(CACHE_VERSION).then((cache) => batchAddUrls(cache, APP_SHELL, 6))
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys()
      .then((keys) => Promise.all(keys.filter((key) => key !== CACHE_VERSION).map((key) => caches.delete(key))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener('fetch', (event) => {
  const { request } = event;
  const url = new URL(request.url);

  if (request.method !== 'GET' || url.origin !== self.location.origin) return;

  const cacheKey = normalizedRequestKey(request);
  const shouldNetworkFirst =
    request.destination === 'script' ||
    request.destination === 'style' ||
    url.pathname === '/index.html' ||
    url.pathname.startsWith('/js/') ||
    url.pathname.startsWith('/css/');

  if (request.mode === 'navigate') {
    event.respondWith(
      fetch(new Request(request, { cache: 'reload' }))
        .then(async (response) => {
          const copy = response.clone();
          const cache = await caches.open(CACHE_VERSION);
          await cache.put('/index.html', copy);
          await trimCache(cache, MAX_CACHE_ENTRIES);
          return response;
        })
        .catch(() => caches.match('/index.html'))
    );
    return;
  }

  if (shouldNetworkFirst) {
    event.respondWith(
      fetch(new Request(request, { cache: 'reload' }))
        .then(async (response) => {
          if (response.ok) {
            const copy = response.clone();
            const cache = await caches.open(CACHE_VERSION);
            await cache.put(cacheKey, copy);
            await trimCache(cache, MAX_CACHE_ENTRIES);
          }
          return response;
        })
        .catch(() => caches.match(cacheKey).then((cached) => cached || caches.match(request)))
    );
    return;
  }

  event.respondWith(
    caches.match(request).then(async (cached) => {
      const network = fetch(request).then(async (response) => {
        if (response.ok) {
          const copy = response.clone();
          const cache = await caches.open(CACHE_VERSION);
          await cache.put(request, copy);
          await trimCache(cache, MAX_CACHE_ENTRIES);
        }
        return response;
      });
      return cached || network;
    })
  );
});
